# 生产部署时，配置公网可达的信令服务器地址（用于跨设备 WebRTC 组网）。
# 开发环境无需配置：默认连本机 ws://<当前主机>:8787（运行 npm run dev:all 即自带）。
# 例如：
# VITE_SIGNALING_URL=wss://signal.example.com

# 信令鉴权（G-SEC-1）：服务器端设 SIGNAL_TOKEN 后，客户端必须配同值 VITE_SIGNAL_TOKEN 才能加入。
# 公网部署务必两边都设；服务器只听明文 ws，公网请挂反向代理（Caddy/Nginx）走 wss。
# SIGNAL_TOKEN=换成足够长的随机串        # 服务器（node server/signaling.js 进程的环境变量）
# VITE_SIGNAL_TOKEN=与上面相同的值        # 前端构建时注入
